# Copy to .env and fill in. The real .env is gitignored: the repository says
# how to deploy, never where to.

# The domain Caddy requests a certificate for, and the base of every canonical
# link, sitemap entry and share image URL.
RETROPDF_DOMAIN=example.com
